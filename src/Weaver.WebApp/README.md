# Weaver WebApp
The front-end for the Weaver project. 

## Deployment
When deploying with docker, make sure to add the `APP_ENV_PREFIX` environment variable to the docker compose!
The value should be `WEAVER_`