import { DashedEdge } from "./dashed-edge/dashed-edge";
import { SelectableEdge } from "./selectable-edge/selectable-edge";

export const EdgeTypes = {
    selectableEdge: SelectableEdge,
    dashedEdge: DashedEdge
}